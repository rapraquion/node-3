function newComment(req, res) {
    const db = req.app.get('db');

}

function editComment(req, res) {
    const db = req.app.get('db');

}

function updatePost(req, res) {
    const db = req.app.get('db');
}

module.exports = { newComment, editComment, updatePost }